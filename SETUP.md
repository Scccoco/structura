# Инструкция по первоначальной настройке

## Шаг 1: Проверка требований

### Windows
- ✅ Docker Desktop установлен и запущен
- ✅ Минимум 4 GB свободной RAM
- ✅ 10 GB свободного места на диске
- ✅ Порты 80, 443, 8080 свободны

### Linux/Mac
- ✅ Docker Engine установлен
- ✅ Docker Compose установлен (или docker compose plugin)
- ✅ Минимум 4 GB свободной RAM
- ✅ 10 GB свободного места на диске
- ✅ Порты 80, 443, 8080 свободны

## Шаг 2: Настройка переменных окружения

1. Скопируйте `.env.example` в `.env`:
   ```bash
   # Windows PowerShell
   Copy-Item .env.example .env
   
   # Linux/Mac
   cp .env.example .env
   ```

2. Откройте `.env` и измените пароли (рекомендуется):
   ```env
   POSTGRES_BIM_PASSWORD=ваш_надежный_пароль
   POSTGRES_NC_PASSWORD=ваш_надежный_пароль
   REDIS_PASSWORD=ваш_надежный_пароль
   NEXTCLOUD_ADMIN_PASSWORD=ваш_надежный_пароль
   ```

## Шаг 3: Запуск стека

### Windows
```powershell
.\start.ps1
```

### Linux/Mac
```bash
chmod +x start.sh stop.sh reset.sh
./start.sh
```

## Шаг 4: Ожидание запуска

Первый запуск может занять 2-5 минут. Проверьте статус:

```bash
cd docker
docker compose ps
```

Все контейнеры должны быть в статусе "Up" или "Up (healthy)".

## Шаг 5: Проверка доступности

Откройте в браузере:
- ✅ http://localhost:8080 - Traefik Dashboard (должен открыться)
- ✅ http://localhost/portainer - Portainer (может потребоваться создание администратора)

## Шаг 6: Настройка Nextcloud

1. Откройте http://localhost/nextcloud
2. Создайте администратора:
   - Логин: `admin` (или из .env)
   - Пароль: `admin` (или из .env)
3. Завершите первоначальную настройку

## Шаг 7: Настройка BIMserver

1. Откройте http://localhost/bimserver
2. При первом запуске создайте администратора
3. Создайте новый проект:
   - Projects → New Project
   - Введите название
4. Загрузите тестовый IFC-файл:
   - Выберите проект
   - Checkin → Upload IFC file
   - Выберите файл

## Шаг 8: Тестирование Viewer

1. Откройте http://localhost/viewer
2. Нажмите "Список проектов"
3. Скопируйте Project ID и Revision ID
4. Введите их в форму
5. Нажмите "Загрузить модель"

## Решение проблем при первом запуске

### Проблема: Порты заняты

**Решение:**
```bash
# Windows - проверка портов
netstat -ano | findstr :80

# Linux/Mac - проверка портов
sudo lsof -i :80

# Остановите конфликтующие сервисы или измените порты в docker-compose.yml
```

### Проблема: Контейнеры не запускаются

**Решение:**
```bash
cd docker
docker compose logs
# Проверьте логи конкретного сервиса
docker compose logs bimserver
docker compose logs nextcloud
```

### Проблема: Недостаточно памяти

**Решение:**
- Увеличьте лимит памяти Docker Desktop (Settings → Resources → Memory)
- Рекомендуется минимум 4 GB, лучше 8 GB

### Проблема: BIMserver не подключается к базе данных

**Решение:**
1. Проверьте, что `postgres_bim` запущен: `docker compose ps`
2. Проверьте логи: `docker compose logs postgres_bim`
3. Проверьте переменные окружения в `.env`
4. Перезапустите: `docker compose restart bimserver`

### Проблема: Nextcloud показывает ошибки

**Решение:**
1. Проверьте логи: `docker compose logs nextcloud`
2. Убедитесь, что `postgres_nc` и `redis` запущены
3. Проверьте права доступа к `data/nextcloud`
4. Перезапустите: `docker compose restart nextcloud`

### Проблема: Viewer не загружает модели

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки в консоли
3. Убедитесь, что модель загружена в BIMserver
4. Проверьте правильность Project ID и Revision ID
5. Проверьте доступность BIMserver: http://localhost/bimserver

## Следующие шаги

После успешного запуска:

1. ✅ Изучите [README.md](README.md) для подробной документации
2. ✅ Прочитайте [ARCHITECTURE.md](ARCHITECTURE.md) для понимания архитектуры
3. ✅ Настройте резервное копирование
4. ✅ Измените пароли по умолчанию
5. ✅ Настройте мониторинг (опционально)

## Поддержка

При возникновении проблем:
1. Проверьте логи контейнеров
2. Изучите документацию компонентов
3. Проверьте требования системы

---

**Удачной работы! 🚀**

