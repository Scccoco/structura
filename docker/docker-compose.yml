services:
  # Traefik - Reverse Proxy с HTTPS
  traefik:
    image: traefik:v2.11
    container_name: traefik
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"  # Traefik dashboard
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik/traefik.yml:/traefik.yml:ro
      - ./traefik/dynamic.yml:/dynamic.yml:ro
      - ./traefik/certs:/certs:ro
      - ../data/traefik:/data
    networks:
      - web
    command:
      - --api.dashboard=true
      - --api.insecure=true
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --providers.file.filename=/dynamic.yml
      - --providers.file.watch=true
      - --entrypoints.web.address=:80
      - --entrypoints.websecure.address=:443
      - --certificatesresolvers.letsencrypt.acme.tlschallenge=true
      - --certificatesresolvers.letsencrypt.acme.storage=/data/acme.json
      - --log.level=INFO
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.traefik.rule=Host(`traefik.localhost`)"
      - "traefik.http.routers.traefik.entrypoints=web"
      - "traefik.http.services.traefik.loadbalancer.server.port=8080"

  # Portainer - UI для управления Docker
  portainer:
    image: portainer/portainer-ce:latest
    container_name: portainer
    restart: unless-stopped
    ports:
      - "9000:9000"  # Прямой доступ к Portainer
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ../data/portainer:/data
    networks:
      - web
    # Portainer не поддерживает работу с подпутями, используем прямой порт

  # PostgreSQL для BIMserver
  postgres_bim:
    image: postgres:15-alpine
    container_name: postgres_bim
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_BIM_DB:-bimserver}
      POSTGRES_USER: ${POSTGRES_BIM_USER:-bimserver}
      POSTGRES_PASSWORD: ${POSTGRES_BIM_PASSWORD:-bimserver_pass}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - ../data/postgres_bim:/var/lib/postgresql/data
    networks:
      - internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_BIM_USER:-bimserver}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # PostgreSQL для Nextcloud
  postgres_nc:
    image: postgres:15-alpine
    container_name: postgres_nc
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_NC_DB:-nextcloud}
      POSTGRES_USER: ${POSTGRES_NC_USER:-nextcloud}
      POSTGRES_PASSWORD: ${POSTGRES_NC_PASSWORD:-nextcloud_pass}
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - ../data/postgres_nc:/var/lib/postgresql/data
    networks:
      - internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_NC_USER:-nextcloud}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis - кеш для Nextcloud
  redis:
    image: redis:7-alpine
    container_name: redis
    restart: unless-stopped
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-redis_pass}
    volumes:
      - ../data/redis:/data
    networks:
      - internal
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD:-redis_pass}", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  # BIMserver - хранилище IFC-моделей
  # Используем собственный Dockerfile для сборки образа
  bimserver:
    build:
      context: ./bimserver
      dockerfile: Dockerfile
    container_name: bimserver
    restart: unless-stopped
    environment:
      - CATALINA_OPTS=-Xmx2g -Xms1g -Djava.awt.headless=true
      - BIMSERVER_DATABASE_TYPE=postgresql
      - BIMSERVER_DATABASE_HOST=postgres_bim
      - BIMSERVER_DATABASE_PORT=5432
      - BIMSERVER_DATABASE_NAME=${POSTGRES_BIM_DB:-bimserver}
      - BIMSERVER_DATABASE_USER=${POSTGRES_BIM_USER:-bimserver}
      - BIMSERVER_DATABASE_PASSWORD=${POSTGRES_BIM_PASSWORD:-bimserver_pass}
    volumes:
      - ../data/bimserver:/var/lib/bimserver
      - ../config/bimserver:/bimserver-home
    networks:
      - web
      - internal
    depends_on:
      postgres_bim:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/bimserver/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s
    labels:
      - "traefik.enable=true"
      # Локальный доступ через PathPrefix
      - "traefik.http.routers.bimserver-local.rule=PathPrefix(`/bimserver`)"
      - "traefik.http.routers.bimserver-local.entrypoints=web"
      - "traefik.http.routers.bimserver-local.service=bimserver"
      # Внешний доступ через Host (добавляем префикс /bimserver)
      - "traefik.http.routers.bimserver-external.rule=Host(`bim.structura-most.ru`)"
      - "traefik.http.routers.bimserver-external.entrypoints=web"
      - "traefik.http.routers.bimserver-external.middlewares=bimserver-addprefix"
      - "traefik.http.routers.bimserver-external.service=bimserver"
      # Сервис (общий для обоих роутеров)
      - "traefik.http.services.bimserver.loadbalancer.server.port=8080"
      # Middleware для добавления /bimserver при внешнем доступе
      - "traefik.http.middlewares.bimserver-addprefix.addprefix.prefix=/bimserver"

  # Nextcloud - файловый CDE
  nextcloud:
    image: nextcloud:27-apache
    container_name: nextcloud
    restart: unless-stopped
    environment:
      - POSTGRES_HOST=postgres_nc
      - POSTGRES_DB=${POSTGRES_NC_DB:-nextcloud}
      - POSTGRES_USER=${POSTGRES_NC_USER:-nextcloud}
      - POSTGRES_PASSWORD=${POSTGRES_NC_PASSWORD:-nextcloud_pass}
      - REDIS_HOST=redis
      - REDIS_HOST_PASSWORD=${REDIS_PASSWORD:-redis_pass}
      - NEXTCLOUD_TRUSTED_DOMAINS=localhost
      - OVERWRITEHOST=localhost
      - OVERWRITEPROTOCOL=http
      - APACHE_DOCUMENTROOT=/var/www/html
      # Автоматическая установка Nextcloud (через occ)
      - NEXTCLOUD_AUTOINSTALL=true
      - NEXTCLOUD_AUTOINSTALL_DB_TYPE=pgsql
      - NEXTCLOUD_AUTOINSTALL_DB_NAME=nextcloud
      - NEXTCLOUD_AUTOINSTALL_DB_USER=nextcloud
      - NEXTCLOUD_AUTOINSTALL_DB_PASS=nextcloud_pass
      - NEXTCLOUD_AUTOINSTALL_DB_HOST=postgres_nc
      - NEXTCLOUD_AUTOINSTALL_ADMIN_USER=admin
      - NEXTCLOUD_AUTOINSTALL_ADMIN_PASS=admin
      - NEXTCLOUD_AUTOINSTALL_SKIP_BROWSER=true
    volumes:
      - ../data/nextcloud:/var/www/html
    # Убираем монтирование config - Nextcloud создаст его сам с правильными правами
    networks:
      - web
      - internal
    depends_on:
      postgres_nc:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    labels:
      - "traefik.enable=true"
      # Локальный доступ через PathPrefix (запросы идут с /nextcloud)
      - "traefik.http.routers.nextcloud-local.rule=PathPrefix(`/nextcloud`)"
      - "traefik.http.routers.nextcloud-local.entrypoints=web"
      - "traefik.http.routers.nextcloud-local.service=nextcloud"
      # Внешний доступ через Host (добавляем префикс /nextcloud и заголовки)
      - "traefik.http.routers.nextcloud-external.rule=Host(`cloud.structura-most.ru`)"
      - "traefik.http.routers.nextcloud-external.entrypoints=web"
      - "traefik.http.routers.nextcloud-external.middlewares=nextcloud-external-chain"
      - "traefik.http.routers.nextcloud-external.service=nextcloud"
      # Сервис (общий для обоих роутеров)
      - "traefik.http.services.nextcloud.loadbalancer.server.port=80"
      # Middleware для добавления /nextcloud при внешнем доступе
      - "traefik.http.middlewares.nextcloud-addprefix.addprefix.prefix=/nextcloud"
      # Middleware для установки заголовков
      - "traefik.http.middlewares.nextcloud-headers.headers.customrequestheaders.X-Forwarded-Proto=https"
      - "traefik.http.middlewares.nextcloud-headers.headers.customrequestheaders.X-Forwarded-Host=cloud.structura-most.ru"
      # Chain для внешнего доступа
      - "traefik.http.middlewares.nextcloud-external-chain.chain.middlewares=nextcloud-addprefix,nextcloud-headers"

  # xeokit-bim-viewer - веб-просмотрщик IFC
  xeokit:
    image: nginx:alpine
    container_name: xeokit
    restart: unless-stopped
    volumes:
      - ../config/xeokit/nginx.conf:/etc/nginx/nginx.conf:ro
      - ../config/xeokit/viewer:/usr/share/nginx/html:ro
    networks:
      - web
    labels:
      - "traefik.enable=true"
      # Локальный доступ через PathPrefix
      - "traefik.http.routers.xeokit-local.rule=PathPrefix(`/viewer`)"
      - "traefik.http.routers.xeokit-local.entrypoints=web"
      - "traefik.http.routers.xeokit-local.middlewares=xeokit-stripprefix"
      - "traefik.http.routers.xeokit-local.service=xeokit"
      # Внешний доступ через Host
      - "traefik.http.routers.xeokit-external.rule=Host(`viewer.structura-most.ru`)"
      - "traefik.http.routers.xeokit-external.entrypoints=web"
      - "traefik.http.routers.xeokit-external.service=xeokit"
      # Сервис (общий для обоих роутеров)
      - "traefik.http.services.xeokit.loadbalancer.server.port=80"
      # Middleware для удаления /viewer при локальном доступе
      - "traefik.http.middlewares.xeokit-stripprefix.stripprefix.prefixes=/viewer"

networks:
  web:
    name: bim_web
    driver: bridge
  internal:
    name: bim_internal
    driver: bridge
    internal: false  # Разрешаем доступ из web сети

volumes:
  postgres_bim_data:
  postgres_nc_data:
  redis_data:
  bimserver_data:
  nextcloud_data:

