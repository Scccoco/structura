# Первые шаги после запуска стека

## ✅ Стек успешно запущен!

Все контейнеры работают:
- ✅ **Traefik** (reverse proxy, SSL)
- ✅ **Refine Frontend** (Пользовательский интерфейс)
- ✅ **Speckle Server** (BIM ядро)
- ✅ **Nextcloud** (Файловое хранилище)
- ✅ **PostgreSQL** (Базы данных `structura` и `speckle`)
- ✅ **Redis** (Кеш)
- ✅ **MinIO** (S3 хранилище)

---

## 🚀 Настройка сервисов

### 1. Nextcloud (https://cloud.structura-most.ru)

*Файловое хранилище для проектов (Акты, Сметы, Чертежи).*

**Первая настройка:**
1. Откройте https://cloud.structura-most.ru (или http://localhost/nextcloud локально)
2. Создайте администратора:
   - Логин: `admin`
   - Пароль: `admin` (или тот, что задали в `.env`)
3. **Важно:** При первой настройке выберите базу данных **PostgreSQL**.
   - Пользователь: `nextcloud`
   - Пароль: `nextcloud_pass`
   - Имя БД: `nextcloud`
   - Хост: `postgres_nc:5432`

### 2. Speckle Server (https://speckle.structura-most.ru)

*BIM-сервер для хранения моделей.*

**Первая настройка:**
1. Откройте https://speckle.structura-most.ru (или http://localhost/speckle локально)
2. Создайте учетную запись администратора при первом входе.
3. Этот сервер будет служить источником данных для Digital Twin.

### 3. Structura App (https://app.structura-most.ru)

*Главный интерфейс системы (Refine).*

**Вход:**
1. Откройте https://app.structura-most.ru (или http://localhost:5173 для dev-режима)
2. Используйте тестовые учетные записи (или настроенные через Supabase/PostgREST).
3. Здесь вы увидите:
   - Реестр сменных заданий
   - Акты исполнительной документации
   - 3D Viewer (интегрированный Speckle)

---

## 📊 Проверка статуса

### Через Portainer (https://port.structura-most.ru)
- Удобный UI для управления Docker контейнерами.
- При первом входе задайте пароль администратора.

### Через командную строку
```powershell
cd c:\structura\docker
docker-compose -f docker-compose.base.yml -f docker-compose.dev.yml ps
```

## 🔍 Просмотр логов

Если что-то не работает, проверяйте логи конкретного сервиса:

```powershell
# Логи бэкенда (Speckle Adapter)
docker logs speckle-adapter

# Логи базы данных
docker logs postgres_speckle

# Логи Nextcloud
docker logs nextcloud
```

## ⚠️ Важные замечания

1. **Пароли:** В файле `CREDENTIALS.md` и `.env` находятся пароли. Обязательно смените их для продакшена!
2. **Данные:** Вся информация хранится в папке `c:\structura\data`. Регулярно делайте бэкапы этой папки.
3. **HTTPS:** На локальной машине сертификаты могут быть недействительны, добавьте исключение в браузере. На VPS Traefik получит легитимные сертификаты автоматически.
