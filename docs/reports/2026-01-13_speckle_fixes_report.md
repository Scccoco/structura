# Технический Отчет: Устранение Проблем Speckle Server

**Дата:** 13 января 2026  
**Проект:** Structura - Digital Twin Platform  
**Инженер:** AI Assistant  
**Для:** Архитектор системы

---

## Исполнительное Резюме

В ходе работы были устранены критические проблемы с аутентификацией и загрузкой IFC моделей в Speckle Server. Реализованы меры по предотвращению падения сервера при обработке больших файлов. Настроен альтернативный метод загрузки через Tekla Connector.

**Ключевые достижения:**
- ✅ Исправлена внешняя аутентификация (ошибка 401/404)
- ✅ Устранена ошибка 403 Forbidden при загрузке IFC
- ✅ Добавлена защита от падения сервера (лимиты ресурсов)
- ✅ Настроен Tekla Connector для прямой загрузки

---

## Проблема 1: Ошибка Аутентификации "Cannot GET /authn/login"

### Симптомы
- Вход работал только с локальной машины (VPS)
- При попытке входа с внешних устройств: ошибка 404
- Cookies не устанавливались корректно

### Причина
1. **Отсутствие заголовка X-Forwarded-Proto: https**
   - Speckle Server не понимал, что работает за HTTPS прокси
   - Устанавливал cookies без флага Secure
   - CSRF-токены не проходили валидацию

2. **Некорректный роутинг Traefik**
   - PathPrefix `/auth` перехватывал пути `/authn/login`
   - Запросы к фронтенду попадали в API backend

### Решение

**Файл:** `docker-compose.prod.yml`

```yaml
# Добавлены метки для speckle-server
labels:
  - "traefik.http.routers.speckle-api-secure.middlewares=speckle-headers"
  - "traefik.http.middlewares.speckle-headers.headers.customrequestheaders.X-Forwarded-Proto=https"
  
# Исправлен PathPrefix
- "traefik.http.routers.speckle-api.rule=Host(`speckle.structura-most.ru`) && PathPrefix(`/graphql`, `/api`, `/objects`, `/auth/`, `/explorer`)"
```

**Результат:** ✅ Аутентификация работает с любых устройств

---

## Проблема 2: Ошибка 403 Forbidden При Загрузке IFC

### Симптомы
- Малые файлы (27 MB) загружались успешно
- Большие файлы (87 MB) зависали на "Uploading new version"
- Логи `speckle-fileimport` показывали:
  ```
  403 (Forbidden) - Response status code does not indicate success
  Error: "Your auth token does not have the required scope: workspace:read"
  ```

### Причина
Конфликт конфигурации `DISABLE_WORKSPACES=true` с версией Speckle Server 2.25.5, которая активно использует систему Workspaces. Fileimport-service не имел необходимых прав для записи данных.

### Решение

**Файл:** `docker-compose.prod.yml`

```yaml
# Закомментирована строка
# DISABLE_WORKSPACES=true  # Отключено - вызывает 403 в версии 2.25.5
```

**Результат:** ✅ Ошибка 403 устранена

---

## Проблема 3: Падение Сервера При Загрузке Больших IFC

### Симптомы
- При загрузке 87 MB IFC файла сервер полностью зависал
- Падали все сервисы (Nextcloud, Speckle, Frontend)
- SSH доступ терял связь

### Причина
Fileimport контейнер без ограничений использовал всю доступную RAM (12GB). При обработке больших файлов:
1. Съедал всю память
2. OOM Killer убивал процессы
3. Docker daemon зависал

### Решение

**Файл:** `docker-compose.prod.yml`

```yaml
speckle-fileimport:
  environment:
    - NODE_OPTIONS=--max-old-space-size=4096  # 4GB heap для Node.js
  deploy:
    resources:
      limits:
        memory: 6G      # Максимум 6GB памяти
        cpus: '2.0'     # Максимум 2 CPU cores
      reservations:
        memory: 1G      # Минимум 1GB
```

**Результат:** 
- ✅ Сервер больше не падает при обработке больших файлов
- ✅ Другие сервисы продолжают работать
- ⚠️ 87 MB файл всё равно не загружается (ошибка ChannelClosedException)

---

## Проблема 4: ChannelClosedException При Отправке Данных

### Симптомы
- Файл успешно скачивается и конвертируется
- При отправке данных в Speckle Server: соединение закрывается
- Ошибка: `System.Threading.Channels.ChannelClosedException`

### Причина
Слишком большое количество объектов в модели. GraphQL API закрывает соединение из-за таймаута при попытке отправить все данные за один запрос.

### Решение

**Рекомендовано использовать Tekla Connector:**

1. **Преимущества:**
   - Прямая отправка без IFC конвертации
   - Оптимизированный протокол Speckle
   - Можно отправлять частями (по категориям)
   - Нет лимитов как у веб-загрузки

2. **Настройка:**
   ```
   Server URL: https://speckle.structura-most.ru
   Auth Method: Legacy Sign In (Personal Access Token)
   Token: b47015ff123fc23131070342b14043c1b8a657dfb7
   ```

3. **Personal Access Token создан:**
   - Name: `test`
   - Scopes: `streams:read`, `streams:write`, `profile:read`
   - Сохранен в `CREDENTIALS.md`

**Альтернатива:** Использовать IFC 2x3 формат и разделить модель на логические части (Архитектура, Конструкции, МЭП).

---

## Внесенные Изменения

### 1. `docker-compose.prod.yml`

**Строки 170-175:** Исправлен роутинг и добавлены headers
```diff
- PathPrefix(`/auth`)
+ PathPrefix(`/auth/`)
+ middlewares=speckle-headers
+ X-Forwarded-Proto=https
```

**Строка 156:** Убран DISABLE_WORKSPACES
```diff
- DISABLE_WORKSPACES=true
+ # DISABLE_WORKSPACES=true
```

**Строки 239-245:** Добавлены лимиты ресурсов
```yaml
+ NODE_OPTIONS=--max-old-space-size=4096
+ deploy:
+   resources:
+     limits:
+       memory: 6G
+       cpus: '2.0'
```

### 2. `.env`

Добавлены явные переменные окружения:
```env
POSTGRES_SPECKLE_DB=speckle
POSTGRES_SPECKLE_USER=speckle
POSTGRES_SPECKLE_PASSWORD=speckle_pass
SPECKLE_SESSION_SECRET=change_this_to_random_32_character_string
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
```

⚠️ **ВАЖНО:** `SPECKLE_SESSION_SECRET` должен быть заменен на случайную строку в продакшене!

### 3. `CREDENTIALS.md`

Добавлен Personal Access Token для Tekla Connector в секцию Speckle Server.

---

## Процесс Деплоя

Все изменения развертывались по стандартной процедуре:

**Локально:**
```bash
git add docker-compose.prod.yml .env
git commit -m "feat: исправления Speckle auth и limits"
git push origin main
```

**На VPS:**
```bash
cd /root/structura
git pull
./update_server.sh
```

Скрипт `update_server.sh` автоматически:
- Останавливает контейнеры
- Пересобирает образы
- Запускает весь стек с новой конфигурацией
- Чистит неиспользуемые образы

---

## Рекомендации

### Краткосрочные (Critical)

1. **Заменить SPECKLE_SESSION_SECRET**
   ```bash
   # Сгенерировать случайную строку
   openssl rand -base64 32
   # Обновить в .env
   ```

2. **Использовать Tekla Connector** для загрузки больших моделей вместо веб-интерфейса

3. **Проверить лимиты памяти** после загрузки: `docker stats speckle-fileimport`

### Среднесрочные (Important)

1. **Настроить мониторинг ресурсов**
   - Установить Prometheus + Grafana
   - Алерты при превышении 80% памяти/CPU

2. **Оптимизировать конфигурацию для больших файлов**
   - Увеличить таймауты GraphQL API
   - Настроить chunked upload в fileimport

3. **Документировать процесс загрузки**
   - Инструкция для пользователей
   - Лимиты размера файлов
   - Рекомендуемые форматы (IFC 2x3)

### Долгосрочные (Nice to Have)

1. **Миграция на новую версию Speckle**
   - Проверить совместимость
   - Обновить через staging сначала

2. **Настроить автомасштабирование**
   - Docker Swarm или Kubernetes
   - Auto-scaling для fileimport

3. **Отдельный сервер для обработки файлов**
   - Выделенная VM для fileimport
   - Балансировка нагрузки

---

## Тестирование

### Успешные Тесты ✅

- Вход с внешних устройств (телефон, другой компьютер)
- Загрузка 12 MB IFC файла через веб-интерфейс
- Загрузка 27 MB IFC файла через веб-интерфейс
- Стабильность сервера при нагрузке

### Частично Успешные ⚠️

- 87 MB IFC файл:
  - ✅ Скачивание и конвертация работают
  - ❌ Отправка данных в Speckle Server зависает
  - **Решение:** Использовать Tekla Connector

### Не Протестировано 🔄

- Загрузка через Tekla Connector (ожидает действия пользователя)
- Работа под длительной нагрузкой (24+ часа)
- Одновременная загрузка нескольких файлов

---

## Известные Ограничения

1. **Размер IFC файлов через веб:** ~30 MB безопасный лимит
2. **RAM сервера:** 12 GB (6 GB доступно для fileimport)
3. **Формат:** IFC 2x3 предпочтительнее IFC4
4. **Таймаут загрузки:** 60 минут (FILE_IMPORT_TIME_LIMIT_MIN)

---

## Контакты и Поддержка

**VPS:**
- IP: `109.73.194.38`
- SSH: `ssh root@109.73.194.38`

**Сервисы:**
- Speckle: https://speckle.structura-most.ru
- Nextcloud: https://cloud.structura-most.ru
- Frontend: https://app.structura-most.ru

**Документация:**
- Credentials: `/c/structura/CREDENTIALS.md`
- Конфигурация: `/c/structura/docker/docker-compose.prod.yml`

---

## Приложения

### A. Логи Типичных Ошибок

**До исправления - 403 Forbidden:**
```
Error: 403 (Forbidden) - Response status code does not indicate success
Error: "Your auth token does not have the required scope: workspace:read"
```

**До исправления - OutOfMemory:**
```
IFC Importer failed with exception Exception of type 'System.OutOfMemoryException' was thrown.
```

**Текущее - ChannelClosedException:**
```
Exception of type 'System.Threading.Channels.ChannelClosedException' was thrown.
```

### B. Конфигурация Ресурсов

| Контейнер | CPU Limit | Memory Limit | Memory Reserve |
|-----------|-----------|--------------|----------------|
| speckle-fileimport | 2 cores | 6 GB | 1 GB |
| speckle-server | - | - | - |
| postgres_speckle | - | - | - |
| nextcloud | - | - | - |

### C. Версии ПО

- Speckle Server: 2.25.5 (latest)
- Speckle Fileimport: latest
- PostgreSQL: 15-alpine
- Redis: 7-alpine
- Traefik: v2.11
- Node.js: 18 (в fileimport)

---

**Конец отчета**
