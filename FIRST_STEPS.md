# Первые шаги после запуска стека

## ✅ Стек успешно запущен!

Все контейнеры работают:
- ✅ Traefik (reverse proxy)
- ✅ Portainer (Docker UI)
- ✅ PostgreSQL для BIMserver
- ✅ PostgreSQL для Nextcloud
- ✅ Redis (кеш)
- ✅ BIMserver (IFC хранилище)
- ✅ Nextcloud (файловый CDE)
- ✅ xeokit Viewer (3D просмотр)

## 🚀 Настройка сервисов

### 1. Nextcloud (http://localhost/nextcloud)

**Первая настройка:**
1. Откройте http://localhost/nextcloud в браузере
2. Создайте администратора:
   - Логин: `admin` (или из .env)
   - Пароль: `admin` (или из .env)
3. Выберите базу данных: **PostgreSQL**
4. Заполните данные:
   - Имя пользователя БД: `nextcloud`
   - Пароль БД: `nextcloud_pass`
   - Имя БД: `nextcloud`
   - Хост БД: `postgres_nc:5432`
5. Нажмите "Завершить настройку"

**После настройки:**
- Загрузите тестовые файлы
- Создайте пользователей
- Настройте приложения

### 2. BIMserver (http://localhost/bimserver)

**Первая настройка:**
1. Откройте http://localhost/bimserver
2. Дождитесь полной загрузки (может занять 1-2 минуты при первом запуске)
3. Создайте администратора:
   - Email: ваш email
   - Пароль: надежный пароль
   - Подтвердите пароль
4. Нажмите "Register"

**Работа с моделями:**
1. Создайте проект:
   - Projects → New Project
   - Введите название (например, "Test Project")
   - Нажмите "Create"
2. Загрузите IFC-файл:
   - Выберите созданный проект
   - Checkin → Upload IFC file
   - Выберите файл на компьютере
   - Дождитесь загрузки и обработки
3. Получите ID:
   - После загрузки откройте проект
   - Скопируйте **Project ID** (например, 12345)
   - Откройте последний Revision
   - Скопируйте **Revision ID** (например, 67890)

### 3. xeokit Viewer (http://localhost/viewer)

**Просмотр модели:**
1. Откройте http://localhost/viewer
2. Нажмите кнопку **"Список проектов"**
   - Это загрузит список всех проектов из BIMserver
   - Покажет Project ID и Revision ID
3. Введите данные:
   - **Project ID**: из BIMserver
   - **Revision ID**: из BIMserver
4. Нажмите **"Загрузить модель"**
5. Модель отобразится в 3D-просмотрщике

**Управление видом:**
- Вращение: зажмите левую кнопку мыши и двигайте
- Панорамирование: зажмите среднюю кнопку мыши (колесо)
- Масштабирование: колесо мыши
- Сброс вида: кнопка "Сброс вида"

### 4. Portainer (http://localhost/portainer)

**Первая настройка:**
1. Откройте http://localhost/portainer
2. Создайте администратора:
   - Username: ваш логин
   - Password: надежный пароль
3. Выберите "Get Started"
4. Выберите "Docker" как окружение

**Возможности:**
- Просмотр всех контейнеров
- Просмотр логов в реальном времени
- Мониторинг ресурсов
- Управление образами и volumes

### 5. Traefik Dashboard (http://localhost:8080)

**Просмотр:**
- Откройте http://localhost:8080
- Увидите список всех сервисов
- Статус маршрутизации
- Метрики запросов

## 📊 Проверка статуса

### Через командную строку:
```powershell
cd docker
docker compose ps
```

### Через Portainer:
- Откройте http://localhost/portainer
- Containers → все контейнеры должны быть "Running"

## 🔍 Просмотр логов

### Все сервисы:
```powershell
cd docker
docker compose logs -f
```

### Конкретный сервис:
```powershell
docker compose logs -f bimserver
docker compose logs -f nextcloud
docker compose logs -f traefik
```

## ⚠️ Важные замечания

1. **BIMserver** при первом запуске может занять 2-5 минут для инициализации
2. **Nextcloud** требует настройки базы данных при первом запуске
3. Все пароли по умолчанию - измените их для продакшена!
4. Данные хранятся в папке `data/` - не удаляйте её

## 🎯 Следующие шаги

1. ✅ Настройте Nextcloud
2. ✅ Настройте BIMserver
3. ✅ Загрузите тестовый IFC-файл
4. ✅ Откройте модель в xeokit Viewer
5. ✅ Изучите Portainer для управления
6. ✅ Измените пароли по умолчанию

## 🆘 Если что-то не работает

1. Проверьте логи: `docker compose logs [service_name]`
2. Проверьте статус: `docker compose ps`
3. Перезапустите сервис: `docker compose restart [service_name]`
4. См. TROUBLESHOOTING.md для деталей

---

**Удачной работы! 🚀**

