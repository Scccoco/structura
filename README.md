# BIM-стек для локальной разработки

Полнофункциональный стек для работы с BIM-данными, включающий хранение IFC-моделей, файловый CDE, 3D-просмотр и управление через единый интерфейс.

## 🏗️ Архитектура проекта

Проект представляет собой изолированный Docker-стек, объединяющий:

- **BIMserver** - хранилище и управление IFC-моделями
- **Nextcloud** - файловый CDE (Common Data Environment) с управлением пользователями
- **xeokit-bim-viewer** - веб-просмотрщик 3D-моделей в браузере
- **Traefik** - reverse proxy с автоматической маршрутизацией
- **Portainer** - веб-интерфейс для управления Docker-контейнерами
- **PostgreSQL** - централизованные базы данных (отдельные для BIMserver и Nextcloud)
- **Redis** - кеширование и блокировки для Nextcloud

## 📁 Структура проекта

```
structura/
├── docker/
│   ├── docker-compose.yml      # Основной файл оркестрации
│   └── traefik/
│       ├── traefik.yml         # Конфигурация Traefik
│       └── dynamic.yml         # Динамические маршруты
├── config/
│   ├── traefik/                # Конфигурация Traefik
│   ├── bimserver/              # Конфигурация BIMserver
│   ├── nextcloud/              # Конфигурация Nextcloud
│   └── xeokit/
│       ├── nginx.conf          # Nginx для viewer
│       └── viewer/
│           └── index.html      # Веб-интерфейс viewer
├── data/                       # Постоянные данные (volumes)
│   ├── traefik/
│   ├── portainer/
│   ├── postgres_bim/
│   ├── postgres_nc/
│   ├── redis/
│   ├── bimserver/
│   └── nextcloud/
├── .env                        # Переменные окружения
├── .env.example                # Пример переменных окружения
├── start.sh                    # Скрипт запуска
├── stop.sh                     # Скрипт остановки
├── reset.sh                    # Скрипт сброса данных
└── README.md                   # Документация
```

## 🚀 Быстрый старт

### Требования

- Docker Desktop (Windows/Mac) или Docker Engine + Docker Compose (Linux)
- Минимум 4 GB свободной RAM
- 10 GB свободного места на диске

### Установка и запуск

1. **Клонируйте или скачайте проект**

2. **Создайте файл `.env`** с переменными окружения:
   ```env
   # PostgreSQL для BIMserver
   POSTGRES_BIM_DB=bimserver
   POSTGRES_BIM_USER=bimserver
   POSTGRES_BIM_PASSWORD=bimserver_pass

   # PostgreSQL для Nextcloud
   POSTGRES_NC_DB=nextcloud
   POSTGRES_NC_USER=nextcloud
   POSTGRES_NC_PASSWORD=nextcloud_pass

   # Redis
   REDIS_PASSWORD=redis_pass

   # Nextcloud
   NEXTCLOUD_ADMIN_USER=admin
   NEXTCLOUD_ADMIN_PASSWORD=admin
   ```
   
   Или скопируйте из примера (если есть):
   ```bash
   # Windows
   Copy-Item .env.example .env
   
   # Linux/Mac
   cp .env.example .env
   ```
   
   **Важно**: Измените пароли по умолчанию для продакшена!

3. **Запустите стек**
   ```bash
   # Linux/Mac
   chmod +x start.sh
   ./start.sh
   
   # Windows (PowerShell)
   .\start.sh
   ```

4. **Дождитесь запуска всех сервисов** (первый запуск может занять 2-3 минуты)

5. **Откройте в браузере:**
   - Nextcloud: http://localhost:8081 (прямой доступ) или http://localhost/nextcloud (через Traefik)
   - BIMserver: http://localhost/bimserver
   - xeokit Viewer: http://localhost/viewer
   - Portainer: http://localhost/portainer
   - Traefik Dashboard: http://localhost:8080

## 📖 Использование

### Nextcloud

1. При первом запуске создайте администратора:
   - Логин: `admin` (или из .env)
   - Пароль: `admin` (или из .env)

2. Загружайте файлы через веб-интерфейс или WebDAV

3. Настройте пользователей и группы через интерфейс администрирования

### BIMserver

1. Откройте http://localhost/bimserver

2. Войдите с учетными данными по умолчанию или создайте нового пользователя

3. Создайте новый проект:
   - Нажмите "Projects" → "New Project"
   - Введите название проекта

4. Загрузите IFC-файл:
   - Выберите проект
   - Нажмите "Checkin" → "Upload IFC file"
   - Выберите файл и дождитесь загрузки

5. После загрузки получите:
   - **Project ID** - идентификатор проекта
   - **Model ID** (Revision ID) - идентификатор модели

### xeokit Viewer

1. Откройте http://localhost/viewer

2. Получите Project ID и Model ID из BIMserver (см. выше)

3. Введите их в форму на странице viewer

4. Нажмите "Загрузить модель"

5. Модель отобразится в 3D-просмотрщике

### Portainer

- Управление контейнерами, образами, сетями и volumes
- Просмотр логов в реальном времени
- Мониторинг ресурсов

## 🔧 Конфигурация

### Изменение паролей

Отредактируйте файл `.env`:

```env
POSTGRES_BIM_PASSWORD=ваш_пароль
POSTGRES_NC_PASSWORD=ваш_пароль
REDIS_PASSWORD=ваш_пароль
NEXTCLOUD_ADMIN_PASSWORD=ваш_пароль
```

После изменения перезапустите стек:
```bash
./stop.sh
./start.sh
```

### Настройка Traefik

Маршруты настраиваются автоматически через labels в `docker-compose.yml`. Для изменения маршрутов отредактируйте:
- `docker/docker-compose.yml` - labels контейнеров
- `docker/traefik/dynamic.yml` - правила маршрутизации

### Настройка BIMserver

Конфигурационные файлы находятся в `config/bimserver/`. Данные хранятся в `data/bimserver/`.

### Настройка Nextcloud

Конфигурация находится в `config/nextcloud/`. Данные пользователей в `data/nextcloud/`.

## 📂 Где хранятся данные

- **Базы данных**: `data/postgres_bim/`, `data/postgres_nc/`
- **IFC-модели**: `data/bimserver/`
- **Файлы Nextcloud**: `data/nextcloud/`
- **Кеш Redis**: `data/redis/`
- **Конфигурации**: `config/`

## 🛠️ Управление стеком

### Запуск
```bash
./start.sh
```

### Остановка
```bash
./stop.sh
```

### Просмотр логов
```bash
cd docker
docker compose logs -f [service_name]
# или
docker-compose logs -f [service_name]
```

### Просмотр статуса
```bash
cd docker
docker compose ps
# или
docker-compose ps
```

### Сброс всех данных
```bash
./reset.sh
```
**ВНИМАНИЕ**: Это удалит все данные, включая базы данных, файлы и модели!

## 🔍 Решение проблем

### Контейнеры не запускаются

1. Проверьте, что порты 80, 443, 8080 свободны:
   ```bash
   # Windows
   netstat -ano | findstr :80
   
   # Linux/Mac
   lsof -i :80
   ```

2. Проверьте логи:
   ```bash
   cd docker
   docker compose logs
   ```

3. Убедитесь, что Docker запущен и имеет достаточно ресурсов

### Nextcloud не подключается к базе данных

1. Проверьте, что `postgres_nc` контейнер запущен и здоров
2. Проверьте переменные окружения в `.env`
3. Проверьте логи Nextcloud: `docker compose logs nextcloud`

### BIMserver не загружает модели

1. Проверьте, что `postgres_bim` контейнер запущен
2. Проверьте логи BIMserver: `docker compose logs bimserver`
3. Убедитесь, что у контейнера достаточно памяти (минимум 2GB)

### xeokit не загружает модели

1. Убедитесь, что модель загружена в BIMserver
2. Проверьте правильность Project ID и Model ID
3. Откройте консоль браузера (F12) для просмотра ошибок
4. Проверьте, что BIMserver доступен по адресу `/bimserver`

### Traefik не маршрутизирует запросы

1. Проверьте логи Traefik: `docker compose logs traefik`
2. Убедитесь, что все сервисы имеют правильные labels
3. Проверьте `docker/traefik/dynamic.yml`

## 🔐 Безопасность

⚠️ **Важно**: Этот стек настроен для **локальной разработки**. Для продакшена:

1. Измените все пароли по умолчанию в `.env`
2. Настройте HTTPS с реальными сертификатами
3. Ограничьте доступ к Portainer и Traefik Dashboard
4. Настройте файрвол
5. Регулярно обновляйте образы контейнеров

## 📝 Версии компонентов

- **Traefik**: 2.11
- **Portainer**: latest
- **PostgreSQL**: 15-alpine
- **Redis**: 7-alpine
- **BIMserver**: 1.5.183 (собирается из Dockerfile на базе Tomcat 9)
- **Nextcloud**: 27-apache
- **Nginx**: alpine (для xeokit)

**Примечание**: BIMserver собирается из собственного Dockerfile при первом запуске. Это может занять несколько минут.

## 🚧 Разработка

### Добавление новых сервисов

1. Добавьте сервис в `docker/docker-compose.yml`
2. Добавьте маршруты в `docker/traefik/dynamic.yml` (если нужен доступ через Traefik)
3. Добавьте labels для автоматической регистрации в Traefik
4. Обновите документацию

### Изменение портов

По умолчанию все сервисы доступны через Traefik на портах 80/443. Для изменения:
1. Отредактируйте `docker-compose.yml`
2. Обновите маршруты в `traefik/dynamic.yml`

## 📄 Лицензия

Этот проект предназначен для локальной разработки и обучения.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи контейнеров
2. Убедитесь, что все требования выполнены
3. Проверьте документацию компонентов:
   - [BIMserver](https://github.com/opensourceBIM/BIMserver)
   - [Nextcloud](https://nextcloud.com/)
   - [xeokit](https://xeokit.github.io/xeokit-bim-viewer/)
   - [Traefik](https://doc.traefik.io/traefik/)

---

**Удачной разработки! 🚀**

